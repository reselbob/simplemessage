# simplemessage
A simple Node.js web server for experimental purposes

# Installation

`git clone https://github.com/reselbob/simplemessage.git`

`npm install`

# Execution

`node index.js`

It's a web app that runs on port `3001` and delivers a message.

The message is defined in the file `message.txt`.
